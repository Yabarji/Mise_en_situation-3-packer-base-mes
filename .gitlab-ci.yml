---
image:
  name: "hashicorp/packer"

before_script:
   - pwd

stages:
  - validate
  - build

validate:
  stage: validate

  script:
    - validate -syntax-only packer.json
    - -v
build:
  stage: build
  script:
    - pwd
    - build packer.json
