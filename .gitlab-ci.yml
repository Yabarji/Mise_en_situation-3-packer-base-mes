---
image:
  name: "hashicorp/packer"

stages:
  - validate
  - build

validate:
  stage: validate

  script:
    - validate -syntax-only packer.json

build:
  stage: build
  script:
    - build packer.json
